services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3012:8080"
    environment:
      - STATIC_DIR=/app/dist
    volumes:
      - ./backend-go/assets/data:/app/assets/data
